import { TransformareSirPipe } from './transformare-sir.pipe';

describe('TransformareSirPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformareSirPipe();
    expect(pipe).toBeTruthy();
  });
});
